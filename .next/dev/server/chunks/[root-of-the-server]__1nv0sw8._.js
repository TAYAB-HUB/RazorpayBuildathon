module.exports = [
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/action-async-storage.external.js [external] (next/dist/server/app-render/action-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/action-async-storage.external.js", () => require("next/dist/server/app-render/action-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/runtime-reacts.external.js [external] (next/dist/server/runtime-reacts.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/runtime-reacts.external.js", () => require("next/dist/server/runtime-reacts.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/node:stream [external] (node:stream, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("node:stream", () => require("node:stream"));

module.exports = mod;
}),
"[project]/src/app/api/batches/[id]/run/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {
__turbopack_context__.s([
    "POST",
    ()=>POST,
    "dynamic",
    ()=>dynamic
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/db/index.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/db/schema.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$sql$2f$expressions$2f$conditions$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/drizzle-orm/sql/expressions/conditions.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$engine$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/engine.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/util.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
;
;
const dynamic = "force-dynamic";
const SEV_RANK = {
    critical: 0,
    high: 1,
    medium: 2,
    low: 3
};
async function POST(req, context) {
    try {
        const params = await Promise.resolve(context.params);
        const id = params.id;
        const body = await req.json().catch(()=>({}));
        const [pm, sm, im, batchRows] = await Promise.all([
            __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["db"].select().from(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["payments"]).where((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$sql$2f$expressions$2f$conditions$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["eq"])(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["payments"].batchId, id)),
            __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["db"].select().from(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["settlements"]).where((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$sql$2f$expressions$2f$conditions$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["eq"])(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["settlements"].batchId, id)),
            __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["db"].select().from(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["invoices"]).where((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$sql$2f$expressions$2f$conditions$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["eq"])(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["invoices"].batchId, id)),
            __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["db"].select().from(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["batches"]).where((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$sql$2f$expressions$2f$conditions$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["eq"])(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["batches"].id, id)).limit(1)
        ]);
        const batch = batchRows[0];
        if (!batch) return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "batch not found"
        }, {
            status: 404
        });
        if (!pm.length) return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "batch has no payments"
        }, {
            status: 400
        });
        // ---- 1. deterministic engine closes the loop ---------------------------
        const result = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$engine$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["runEngine"])({
            payments: pm,
            settlements: sm,
            invoices: im
        });
        // ---- 2. AI only where ambiguity is genuine: adjudication notes ----------
        const useAI = body.useAI !== false;
        let aiMode = "heuristic";
        const payMap = new Map(pm.map((p)=>[
                p.id,
                p
            ]));
        const sorted = [
            ...result.exceptions
        ].sort((a, b)=>(SEV_RANK[a.severity] ?? 4) - (SEV_RANK[b.severity] ?? 4));
        const budget = sorted.slice(0, 8); // cost + latency discipline
        const notes = await Promise.all(budget.map(async (e)=>{
            const p = payMap.get(e.recordId);
            const live = useAI && (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["aiAvailable"])() ? await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["aiAdjudicateException"])({
                category: e.category,
                severity: e.severity,
                detail: e.detail,
                kind: e.kind,
                amountInr: p ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(p.amountPaise) : undefined,
                matchRate: result.stats.matchRateBank
            }) : null;
            return {
                key: `${e.kind}:${e.recordId}:${e.category}`,
                live,
                fallback: (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$engine$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["heuristicNote"])(e)
            };
        }));
        const noteMap = new Map(notes.map((n)=>[
                n.key,
                n
            ]));
        if (notes.some((n)=>n.live)) aiMode = "live";
        for (const e of result.exceptions){
            const n = noteMap.get(`${e.kind}:${e.recordId}:${e.category}`);
            e.aiNote = n ? n.live ?? n.fallback : (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$engine$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["heuristicNote"])(e);
        }
        // ---- 3. measure honestly against hidden ground truth --------------------
        const truth = batch.truth;
        const metrics = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$engine$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["scoreRun"])(result, truth);
        // ---- 4. persist the whole close-out --------------------------------------
        const runId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$engine$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["newRunId"])();
        await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["db"].insert(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["runs"]).values({
            id: runId,
            batchId: id,
            engineVersion: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$engine$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ENGINE_VERSION"],
            aiMode,
            status: result.status,
            wallMs: result.stats.wallMs,
            stats: {
                ...result.stats,
                aiMode,
                aiNotes: notes.filter((n)=>n.live).length,
                invariants: result.invariants
            },
            passes: result.passes,
            metrics,
            cash: result.cash,
            trace: result.trace
        });
        if (result.matches.length) {
            await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["db"].insert(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["matches"]).values(result.matches.map((m)=>({
                    runId,
                    batchId: id,
                    paymentId: m.paymentId,
                    settlementId: m.settlementId,
                    invoiceId: m.invoiceId,
                    rule: m.rule,
                    confidence: m.confidence,
                    explanation: m.explanation,
                    aiUsed: m.aiUsed
                })));
        }
        if (result.exceptions.length) {
            await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["db"].insert(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["exceptions"]).values(result.exceptions.map((e)=>({
                    runId,
                    batchId: id,
                    kind: e.kind,
                    recordId: e.recordId,
                    category: e.category,
                    severity: e.severity,
                    detail: e.detail,
                    aiNote: e.aiNote ?? null,
                    status: "open"
                })));
        }
        await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["db"].insert(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["auditLog"]).values({
            batchId: id,
            runId,
            actor: "engine",
            action: "run_completed",
            payload: {
                engineVersion: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$engine$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ENGINE_VERSION"],
                aiMode,
                matched: result.stats.matchedPayments,
                of: result.stats.payments,
                exceptions: result.stats.exceptionsOpen,
                precision: metrics.bank.precision,
                recall: metrics.bank.recall,
                status: result.status,
                wallMs: result.stats.wallMs
            }
        });
        await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["db"].update(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["batches"]).set({
            status: "reconciled"
        }).where((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$sql$2f$expressions$2f$conditions$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["eq"])(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$db$2f$schema$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["batches"].id, id));
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            runId,
            aiMode,
            status: result.status,
            stats: result.stats
        });
    } catch (e) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: String(e)
        }, {
            status: 500
        });
    }
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/src/db/index.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {
__turbopack_context__.s([
    "db",
    ()=>db,
    "pool",
    ()=>pool
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$node$2d$postgres$2f$driver$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/drizzle-orm/node-postgres/driver.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$pg__$5b$external$5d$__$28$pg$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$pg$29$__ = __turbopack_context__.i("[externals]/pg [external] (pg, esm_import, [project]/node_modules/pg)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$node$2d$postgres$2f$driver$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$externals$5d2f$pg__$5b$external$5d$__$28$pg$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$pg$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$node$2d$postgres$2f$driver$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$externals$5d2f$pg__$5b$external$5d$__$28$pg$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$pg$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
const databaseUrl = process.env.DATABASE_URL;
if (!databaseUrl) {
    throw new Error("DATABASE_URL is required");
}
const globalForDb = globalThis;
const pool = globalForDb.__arenaNextJsPostgresqlPool ?? new __TURBOPACK__imported__module__$5b$externals$5d2f$pg__$5b$external$5d$__$28$pg$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$pg$29$__["Pool"]({
    connectionString: databaseUrl
});
if ("TURBOPACK compile-time truthy", 1) {
    globalForDb.__arenaNextJsPostgresqlPool = pool;
}
const db = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$node$2d$postgres$2f$driver$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["drizzle"])(pool);
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/src/db/schema.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "auditLog",
    ()=>auditLog,
    "batches",
    ()=>batches,
    "exceptions",
    ()=>exceptions,
    "invoices",
    ()=>invoices,
    "matches",
    ()=>matches,
    "payments",
    ()=>payments,
    "runs",
    ()=>runs,
    "settlements",
    ()=>settlements
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$table$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/drizzle-orm/pg-core/table.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/drizzle-orm/pg-core/columns/text.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$integer$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/drizzle-orm/pg-core/columns/integer.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$real$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/drizzle-orm/pg-core/columns/real.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$boolean$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/drizzle-orm/pg-core/columns/boolean.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$timestamp$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/drizzle-orm/pg-core/columns/timestamp.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$serial$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/drizzle-orm/pg-core/columns/serial.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$jsonb$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/drizzle-orm/pg-core/columns/jsonb.js [app-route] (ecmascript)");
;
const batches = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$table$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pgTable"])("batches", {
    id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("id").primaryKey(),
    name: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("name").notNull(),
    seed: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("seed").notNull(),
    status: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("status").notNull().default("generated"),
    counts: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$jsonb$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["jsonb"])("counts").$type(),
    // Ground truth — never read by the matching engine, only by the scorer.
    truth: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$jsonb$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["jsonb"])("truth").$type(),
    createdAt: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$timestamp$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["timestamp"])("created_at", {
        mode: "string"
    }).notNull().defaultNow()
});
const payments = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$table$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pgTable"])("payments", {
    id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("id").primaryKey(),
    batchId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("batch_id").notNull(),
    gatewayRef: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("gateway_ref").notNull(),
    utr: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("utr").notNull(),
    customer: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("customer").notNull(),
    method: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("method").notNull(),
    amountPaise: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$integer$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["integer"])("amount_paise").notNull(),
    feePaise: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$integer$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["integer"])("fee_paise").notNull(),
    status: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("status").notNull().default("captured"),
    day: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("day").notNull()
});
const settlements = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$table$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pgTable"])("settlements", {
    id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("id").primaryKey(),
    batchId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("batch_id").notNull(),
    bankRef: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("bank_ref").notNull(),
    utrRaw: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("utr_raw"),
    amountPaise: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$integer$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["integer"])("amount_paise"),
    dateRaw: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("date_raw").notNull(),
    narration: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("narration").notNull().default("")
});
const invoices = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$table$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pgTable"])("invoices", {
    id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("id").primaryKey(),
    batchId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("batch_id").notNull(),
    invoiceNo: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("invoice_no").notNull(),
    customer: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("customer").notNull(),
    amountPaise: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$integer$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["integer"])("amount_paise").notNull(),
    dueDay: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("due_day").notNull(),
    status: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("status").notNull().default("open")
});
const runs = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$table$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pgTable"])("runs", {
    id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("id").primaryKey(),
    batchId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("batch_id").notNull(),
    engineVersion: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("engine_version").notNull(),
    aiMode: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("ai_mode").notNull(),
    status: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("status").notNull(),
    wallMs: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$integer$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["integer"])("wall_ms").notNull(),
    stats: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$jsonb$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["jsonb"])("stats").notNull(),
    passes: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$jsonb$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["jsonb"])("passes").notNull(),
    metrics: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$jsonb$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["jsonb"])("metrics").notNull(),
    cash: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$jsonb$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["jsonb"])("cash").notNull(),
    trace: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$jsonb$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["jsonb"])("trace").notNull(),
    createdAt: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$timestamp$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["timestamp"])("created_at", {
        mode: "string"
    }).notNull().defaultNow()
});
const matches = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$table$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pgTable"])("matches", {
    id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$serial$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["serial"])("id").primaryKey(),
    runId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("run_id").notNull(),
    batchId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("batch_id").notNull(),
    paymentId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("payment_id"),
    settlementId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("settlement_id"),
    invoiceId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("invoice_id"),
    rule: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("rule").notNull(),
    confidence: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$real$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["real"])("confidence").notNull(),
    explanation: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("explanation").notNull(),
    aiUsed: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$boolean$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["boolean"])("ai_used").notNull().default(false)
});
const exceptions = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$table$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pgTable"])("exceptions", {
    id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$serial$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["serial"])("id").primaryKey(),
    runId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("run_id").notNull(),
    batchId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("batch_id").notNull(),
    kind: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("kind").notNull(),
    recordId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("record_id").notNull(),
    category: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("category").notNull(),
    severity: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("severity").notNull(),
    detail: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("detail").notNull(),
    aiNote: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("ai_note"),
    status: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("status").notNull().default("open"),
    resolution: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("resolution"),
    resolvedAt: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$timestamp$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["timestamp"])("resolved_at", {
        mode: "string"
    })
});
const auditLog = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$table$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pgTable"])("audit_log", {
    id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$serial$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["serial"])("id").primaryKey(),
    batchId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("batch_id").notNull(),
    runId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("run_id"),
    actor: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("actor").notNull(),
    action: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$text$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["text"])("action").notNull(),
    payload: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$jsonb$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["jsonb"])("payload"),
    createdAt: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$drizzle$2d$orm$2f$pg$2d$core$2f$columns$2f$timestamp$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["timestamp"])("created_at", {
        mode: "string"
    }).notNull().defaultNow()
});
}),
"[project]/src/lib/ai.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// ---------------------------------------------------------------------------
// REKON AI layer — deliberately optional.
// The matching engine is deterministic because money demands proof. AI is
// used ONLY where ambiguity is genuine: adjudication notes on exceptions and
// natural-language phrasing of answers. If no key is configured (or the call
// fails/times out) the system falls back to deterministic heuristics and
// plainly labels which mode it ran in. Secrets never leave the server.
// ---------------------------------------------------------------------------
__turbopack_context__.s([
    "aiAdjudicateException",
    ()=>aiAdjudicateException,
    "aiAvailable",
    ()=>aiAvailable,
    "aiPhraseAnswer",
    ()=>aiPhraseAnswer
]);
function aiAvailable() {
    return !!process.env.OPENAI_API_KEY && process.env.AI_DISABLED !== "1";
}
function config() {
    return {
        key: process.env.OPENAI_API_KEY ?? "",
        baseUrl: (process.env.OPENAI_BASE_URL ?? "https://api.openai.com/v1").replace(/\/$/, ""),
        model: process.env.OPENAI_MODEL ?? "gpt-4o-mini"
    };
}
async function chat(system, user, timeoutMs = 6000) {
    if (!aiAvailable()) return null;
    const { key, baseUrl, model } = config();
    const ctrl = new AbortController();
    const timer = setTimeout(()=>ctrl.abort(), timeoutMs);
    try {
        const res = await fetch(`${baseUrl}/chat/completions`, {
            method: "POST",
            headers: {
                "content-type": "application/json",
                authorization: `Bearer ${key}`
            },
            body: JSON.stringify({
                model,
                temperature: 0.2,
                max_tokens: 220,
                messages: [
                    {
                        role: "system",
                        content: system
                    },
                    {
                        role: "user",
                        content: user
                    }
                ]
            }),
            signal: ctrl.signal
        });
        if (!res.ok) return null;
        const data = await res.json();
        const text = data.choices?.[0]?.message?.content?.trim();
        return text && text.length > 0 ? text : null;
    } catch  {
        return null;
    } finally{
        clearTimeout(timer);
    }
}
async function aiAdjudicateException(ctx) {
    const system = "You are the adjudication module of a finance reconciliation controller at a payments company. " + "Write a precise, conservative 2-3 sentence adjudication note: what likely happened, and the single safest next action. " + "Never guess a match for money. Use INR amounts if present. No markdown, no bullet points.";
    const user = JSON.stringify(ctx);
    return chat(system, user);
}
async function aiPhraseAnswer(question, resultJson) {
    const system = "You are the settlement Q&A agent of a finance controller. The user asked a question; you are given the exact computed facts. " + "Answer in 1-3 sentences using ONLY those facts — never invent numbers. Be crisp and operational. No markdown.";
    const user = `Question: ${question}\nFacts: ${resultJson}`;
    return chat(system, user);
}
}),
"[project]/src/lib/engine.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ENGINE_VERSION",
    ()=>ENGINE_VERSION,
    "heuristicNote",
    ()=>heuristicNote,
    "newRunId",
    ()=>newRunId,
    "runEngine",
    ()=>runEngine,
    "scoreRun",
    ()=>scoreRun
]);
// ---------------------------------------------------------------------------
// REKON reconciliation engine — deterministic by design.
//
// Money movement must be *verifiable, replayable and explainable*. So every
// match is produced by a named rule with an auditable explanation, never by a
// model. AI is reserved for the genuinely ambiguous tail (see lib/ai.ts).
//
// Passes (in order — strongest evidence first):
//   P0 quarantine               malformed bank lines are isolated, never crash
//   P1 exact_utr_gross          UTR + exact gross amount
//   P2 net_utr_fee              UTR + fee-adjusted net settlement
//   P3 utr_amount_tolerance     UTR + ₹±8 bank posting noise
//   P4 fuzzy_ref_amount         truncated/typo'd references (trigram+levenshtein)
//   P5 split_pair               two bank lines summing to one settlement
//   P6 amount_date_unique       amount equality within ±2d, unique both sides
//   P7 extended_window          amount equality 3–6d out (delayed settlements)
// Whatever survives becomes an honest, classified exception.
// ---------------------------------------------------------------------------
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$synth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/src/lib/synth.ts [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/util.ts [app-route] (ecmascript)");
;
;
const ENGINE_VERSION = "v1.4.2-deterministic";
// --------------------------------------------------------------------------
const TOL_PAISE = 800; // ±₹8 bank posting tolerance
const BASE_WINDOW = 2;
const EXT_WINDOW = 6;
function runEngine(input) {
    const t0 = performance.now();
    const trace = [];
    const mark = (level, msg)=>trace.push({
            t: Math.round(performance.now() - t0),
            level,
            msg
        });
    const pays = input.payments.map((row)=>({
            row,
            id: row.id,
            utrNorm: (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeRef"])(row.utr),
            gross: row.amountPaise,
            net: row.amountPaise - (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$synth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["expectedFee"])(row.method, row.amountPaise),
            day: row.day,
            matched: false,
            settlementIds: []
        }));
    const lines = input.settlements.map((row)=>{
        const reasons = [];
        if (row.amountPaise === null || row.amountPaise === undefined) reasons.push("null_amount");
        else if (row.amountPaise < 0) reasons.push("negative_amount");
        else if (row.amountPaise === 0) reasons.push("zero_amount");
        const day = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["parseBankDate"])(row.dateRaw);
        if (!day) reasons.push("unparseable_date");
        return {
            row,
            id: row.id,
            utrNorm: (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeRef"])(row.utrRaw),
            amount: row.amountPaise,
            day,
            malformedReasons: reasons,
            matched: false
        };
    });
    const matches = [];
    const exceptions = [];
    const passes = [];
    const abstentions = [];
    mark("info", `ingest ${pays.length} payments · ${lines.length} bank lines · ${input.invoices.length} invoices`);
    const pushPass = (key, label, matched, skipped, started)=>{
        passes.push({
            key,
            label,
            matched,
            skippedAmbiguous: skipped,
            ms: Math.max(1, Math.round(performance.now() - started))
        });
        mark(matched > 0 || skipped > 0 ? "ok" : "info", `${key}: ${matched} matched${skipped > 0 ? ` · ${skipped} abstained (ambiguous)` : ""}`);
    };
    const claim = (p, s, rule, confidence, explanation)=>{
        p.matched = true;
        p.settlementIds.push(s.id);
        s.matched = true;
        s.matchedBy = rule;
        matches.push({
            paymentId: p.id,
            settlementId: s.id,
            invoiceId: null,
            rule,
            confidence,
            explanation,
            aiUsed: false
        });
    };
    // ---- P0: quarantine -----------------------------------------------------
    let started = performance.now();
    const valid = lines.filter((l)=>l.malformedReasons.length === 0);
    const broken = lines.filter((l)=>l.malformedReasons.length > 0);
    for (const b of broken){
        exceptions.push({
            kind: "settlement",
            recordId: b.id,
            category: "MALFORMED",
            severity: "high",
            detail: `Bank line ${b.row.bankRef} quarantined at ingest: ${b.malformedReasons.join(", ")} (raw: amount=${b.row.amountPaise ?? "null"}, date="${b.row.dateRaw}"). Engine continued without it.`
        });
    }
    pushPass("quarantine", "P0 · quarantine malformed lines", 0, 0, started);
    if (broken.length) mark("warn", `quarantined ${broken.length} malformed bank line(s) — isolated from matching`);
    // ---- reference-based passes ---------------------------------------------
    const tryRefPass = (key, label, amountOk)=>{
        started = performance.now();
        let n = 0;
        for (const s of valid){
            if (s.matched || !s.utrNorm) continue;
            const candidates = pays.filter((p)=>!p.matched && p.utrNorm === s.utrNorm);
            for (const p of candidates){
                const verdict = amountOk(p, s);
                if (verdict?.ok) {
                    claim(p, s, key, verdict.conf, `${verdict.why} · ref ${s.row.utrRaw} ↔ ${p.row.utr}`);
                    n++;
                    break;
                }
            }
        }
        pushPass(key, label, n, 0, started);
    };
    tryRefPass("exact_utr_gross", "P1 · UTR + gross amount", (p, s)=>s.amount === p.gross ? {
            ok: true,
            conf: 0.99,
            why: `UTR exact; settled gross ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(p.gross)} (fee billed monthly)`
        } : null);
    tryRefPass("net_utr_fee", "P2 · UTR + fee-adjusted net", (p, s)=>{
        const fee = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$synth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["expectedFee"])(p.row.method, p.gross);
        return s.amount === p.gross - fee ? {
            ok: true,
            conf: 0.96,
            why: `UTR exact; net ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(p.gross - fee)} = gross − ${p.row.method} fee ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(fee)}`
        } : null;
    });
    tryRefPass("utr_amount_tolerance", "P3 · UTR + ±₹8 posting noise", (p, s)=>{
        if (s.amount === null) return null;
        const targets = [
            {
                v: p.gross,
                label: "gross"
            },
            {
                v: p.net,
                label: "net"
            }
        ];
        for (const t of targets){
            const d = Math.abs(s.amount - t.v);
            if (d > 0 && d <= TOL_PAISE) {
                return {
                    ok: true,
                    conf: 0.86 - d / TOL_PAISE / 10,
                    why: `UTR exact; ${t.label} off by ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(d)} — within posting tolerance`
                };
            }
        }
        return null;
    });
    // ---- P4: fuzzy reference -------------------------------------------------
    started = performance.now();
    {
        let n = 0;
        let skipped = 0;
        for (const s of valid){
            if (s.matched || !s.utrNorm) continue;
            const scored = pays.filter((p)=>!p.matched).map((p)=>{
                const sim = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["refSimilarity"])(p.utrNorm, s.utrNorm);
                // exact amount only: pairing a weak reference with loose money is
                // how false positives are born — tolerance demands the strong UTR.
                const amountHit = s.amount !== null && (s.amount === p.gross || s.amount === p.net);
                return {
                    p,
                    sim,
                    amountHit
                };
            }).filter((x)=>x.sim >= 0.65 && x.amountHit).sort((a, b)=>b.sim - a.sim);
            if (scored.length === 1 || scored.length > 1 && scored[0].sim > scored[1].sim + 0.05) {
                const { p, sim } = scored[0];
                claim(p, s, "fuzzy_ref_amount", 0.74 + sim * 0.14, `reference recovered (similarity ${(sim * 100).toFixed(0)}%): “${s.row.utrRaw}” ↔ ${p.row.utr}; amount confirm`);
                n++;
            } else if (scored.length > 1) {
                abstentions.push({
                    paymentId: scored[0].p.id,
                    settlementId: s.id,
                    reason: "fuzzy_ref tie"
                });
                skipped++;
            }
        }
        pushPass("fuzzy_ref_amount", "P4 · fuzzy reference + amount", n, skipped, started);
    }
    /**
   * Contested-amount guard: if two unmatched payments have an identical
   * entitlement (same net or gross), no amount-only evidence may clear either
   * one — the engine abstains and routes both to a human rather than guess.
   */ const contestersOf = (amount)=>pays.filter((q)=>!q.matched && (q.net === amount || q.gross === amount));
    const veto = (contested, s, reason)=>{
        for (const q of contested)abstentions.push({
            paymentId: q.id,
            settlementId: s.id,
            reason
        });
    };
    // ---- P5: split pairs ------------------------------------------------------
    started = performance.now();
    {
        let n = 0;
        let skipped = 0;
        for (const p of pays){
            if (p.matched) continue;
            const pool = valid.filter((s)=>!s.matched && s.amount !== null && s.day !== null && Math.abs((0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["dayDiff"])(s.day, p.day)) <= 4 && s.amount < p.gross // a part can exceed the net when fees are monthly-billed
            );
            let found = null;
            outer: for(let i = 0; i < pool.length; i++){
                for(let j = i + 1; j < pool.length; j++){
                    const a = pool[i];
                    const b = pool[j];
                    const sum = (a.amount ?? 0) + (b.amount ?? 0);
                    if (sum === p.net || sum === p.gross) {
                        found = [
                            a,
                            b
                        ];
                        break outer;
                    }
                }
            }
            if (found) {
                const target = (found[0].amount ?? 0) + (found[1].amount ?? 0);
                const contested = contestersOf(target);
                if (contested.length > 1) {
                    for (const s of found)veto(contested, s, "split sum contested by twins");
                    skipped++;
                    continue;
                }
                const basis = target === p.net ? "net" : "gross";
                for (const s of found){
                    claim(p, s, "split_pair", 0.82, `settlement split across ${found.length} lines; pair sums to ${basis} ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(basis === "net" ? p.net : p.gross)}`);
                }
                n++;
            }
        }
        pushPass("split_pair", "P5 · split settlement pairs", n, skipped, started);
    }
    // ---- P6/P7: amount-only passes with uniqueness guard ---------------------
    const amountWindowPass = (key, label, minDays, maxDays, conf)=>{
        started = performance.now();
        let n = 0;
        let skipped = 0;
        for (const s of valid){
            if (s.matched || s.amount === null || s.day === null) continue;
            const cands = pays.filter((p)=>{
                if (p.matched) return false;
                const d = Math.abs((0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["dayDiff"])(s.day, p.day));
                if (d < minDays || d > maxDays) return false;
                return s.amount === p.gross || s.amount === p.net;
            });
            const contested = contestersOf(s.amount);
            if (cands.length === 1 && contested.length === 1) {
                const p = cands[0];
                const basis = s.amount === p.net ? "net" : "gross";
                claim(p, s, key, conf, `${basis} amount ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(s.amount)} unique candidate; date drift ${Math.abs((0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["dayDiff"])(s.day, p.day))}d`);
                n++;
            } else if (cands.length > 0 && contested.length > 1) {
                // twins share this entitlement and at least one is in-window
                veto(contested, s, "equal entitlement — abstained");
                skipped++;
            }
        }
        pushPass(key, label, n, skipped, started);
    };
    amountWindowPass("amount_date_unique", "P6 · unique amount within ±2d", 0, BASE_WINDOW, 0.9);
    amountWindowPass("extended_window", "P7 · unique amount T+3…T+6 (delayed)", 3, EXT_WINDOW, 0.72);
    // ---- books pass: invoices ↔ payments --------------------------------------
    const invDones = new Set();
    {
        started = performance.now();
        let n = 0;
        for (const inv of input.invoices){
            const cands = pays.filter((p)=>p.row.customer === inv.customer && p.gross === inv.amountPaise);
            if (cands.length === 1) {
                matches.push({
                    paymentId: cands[0].id,
                    settlementId: null,
                    invoiceId: inv.id,
                    rule: "books_customer_amount",
                    confidence: 0.94,
                    explanation: `invoice ${inv.invoiceNo} ↔ payment ${cands[0].row.gatewayRef}: customer + ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(inv.amountPaise)} exact, unique`,
                    aiUsed: false
                });
                invDones.add(inv.id);
                n++;
            }
        }
        pushPass("books_customer_amount", "P8 · books: customer + amount", n, 0, started);
    }
    // ---- classification of what remains — the honest exception list ----------
    const ambiguousSettlementIds = new Set();
    for (const p of pays){
        if (p.matched) continue;
        const abstained = abstentions.filter((a)=>a.paymentId === p.id);
        const refTwin = valid.find((s)=>!s.matched && s.utrNorm && (s.utrNorm === p.utrNorm || (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["refSimilarity"])(s.utrNorm, p.utrNorm) > 0.9));
        const farAmount = valid.find((s)=>!s.matched && s.amount !== null && s.day !== null && (s.amount === p.net || s.amount === p.gross) && Math.abs((0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["dayDiff"])(s.day, p.day)) > EXT_WINDOW);
        if (abstained.length > 0) {
            ambiguousSettlementIds.add(abstained[0].settlementId);
            const twins = abstentions.filter((a)=>a.settlementId === abstained[0].settlementId && a.paymentId !== p.id).map((a)=>a.paymentId);
            exceptions.push({
                kind: "payment",
                recordId: p.id,
                category: "AMBIGUOUS",
                severity: "high",
                detail: `${p.row.gatewayRef} (${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(p.gross)}, ${p.day}) collides with ${twins.length} twin payment(s) [${twins.map((t)=>input.payments.find((x)=>x.id === t)?.gatewayRef ?? t).join(", ")}] for bank line ${input.settlements.find((x)=>x.id === abstained[0].settlementId)?.bankRef}. Engine abstained — guessing is not an option for money.`
            });
        } else if (refTwin) {
            const delta = (refTwin.amount ?? 0) - p.net;
            exceptions.push({
                kind: "payment",
                recordId: p.id,
                category: "AMOUNT_MISMATCH",
                severity: "critical",
                detail: `${p.row.gatewayRef}: UTR matches bank line ${refTwin.row.bankRef} but amount ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(refTwin.amount)} ≠ expected net ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(p.net)} / gross ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(p.gross)} (Δ ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(delta)}). Probable bank-side posting error.`
            });
            ambiguousSettlementIds.add(refTwin.id);
        } else if (farAmount) {
            exceptions.push({
                kind: "payment",
                recordId: p.id,
                category: "DATE_DRIFT",
                severity: "medium",
                detail: `${p.row.gatewayRef} (${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(p.gross)}) settled ${Math.abs((0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["dayDiff"])(farAmount.day, p.day))} days late on ${farAmount.day} (line ${farAmount.row.bankRef}) — outside the T+${EXT_WINDOW} policy window. Likely bank delay, not a loss.`
            });
            ambiguousSettlementIds.add(farAmount.id);
        } else {
            const age = Math.max(0, (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["dayDiff"])(new Date().toISOString().slice(0, 10), p.day));
            exceptions.push({
                kind: "payment",
                recordId: p.id,
                category: "UNSETTLED",
                severity: age >= 5 ? "high" : "medium",
                detail: `${p.row.gatewayRef} captured ${p.day} for ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(p.gross)} has no bank line after ${age}d. Expected net ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(p.net)} by T+2 (${p.row.method} schedule). Aging ${age}d${age >= 5 ? " — escalate" : ""}.`
            });
        }
    }
    for (const s of valid){
        if (s.matched || ambiguousSettlementIds.has(s.id)) continue;
        const nar = s.row.narration.toUpperCase();
        const matchedNorms = new Set(valid.filter((x)=>x.matched).map((x)=>x.utrNorm));
        if (s.utrNorm && matchedNorms.has(s.utrNorm)) {
            exceptions.push({
                kind: "settlement",
                recordId: s.id,
                category: "DUPLICATE",
                severity: "high",
                detail: `Bank line ${s.row.bankRef} (${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(s.amount)}) duplicates an already-matched line for ref ${s.row.utrRaw}. Possible double settlement — funds may need returning.`
            });
        } else if (nar.includes("REFUND")) {
            exceptions.push({
                kind: "settlement",
                recordId: s.id,
                category: "ORPHAN_REFUND",
                severity: "low",
                detail: `Line ${s.row.bankRef} (${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(s.amount)}) is a REFUND debit/credit with no matching original payment in this batch. Routed to refunds ledger.`
            });
        } else if (nar.includes("CHARGEBACK")) {
            exceptions.push({
                kind: "settlement",
                recordId: s.id,
                category: "ORPHAN_CHARGEBACK",
                severity: "critical",
                detail: `Line ${s.row.bankRef} (${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(s.amount)}) carries CHARGEBACK narration with no source payment here. Dispute window is short — act today.`
            });
        } else {
            exceptions.push({
                kind: "settlement",
                recordId: s.id,
                category: "ORPHAN_BANK",
                severity: "medium",
                detail: `Line ${s.row.bankRef} (${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(s.amount)}, ref “${s.row.utrRaw ?? "—"}”) matches no captured payment. Unattributed money is a liability.`
            });
        }
    }
    for (const inv of input.invoices){
        if (invDones.has(inv.id)) continue;
        exceptions.push({
            kind: "invoice",
            recordId: inv.id,
            category: "UNPAID_INVOICE",
            severity: "medium",
            detail: `${inv.invoiceNo} (${inv.customer}, ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(inv.amountPaise)}) due ${inv.dueDay} has no matching payment in the gateway batch.`
        });
    }
    // ---- invariants: the engine checks its own books --------------------------
    const invariants = [];
    const sumMatchedLines = matches.filter((m)=>m.settlementId).reduce((acc, m)=>acc + (valid.find((s)=>s.id === m.settlementId)?.amount ?? 0), 0);
    const sumUnmatchedValid = valid.filter((s)=>!s.matched).reduce((acc, s)=>acc + (s.amount ?? 0), 0);
    const sumValid = valid.reduce((acc, s)=>acc + (s.amount ?? 0), 0);
    invariants.push({
        name: "conservation_of_bank_total",
        ok: Math.abs(sumMatchedLines + sumUnmatchedValid - sumValid) === 0,
        detail: `matched ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(sumMatchedLines)} + unmatched ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(sumUnmatchedValid)} = bank total ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["inr"])(sumValid)}`
    });
    const lineUse = new Map();
    for (const m of matches)if (m.settlementId) lineUse.set(m.settlementId, (lineUse.get(m.settlementId) ?? 0) + 1);
    const overused = [
        ...lineUse.entries()
    ].filter(([, c])=>c > 1 && !matches.some((m)=>m.settlementId && m.rule === "split_pair"));
    invariants.push({
        name: "no_double_settlement_spend",
        ok: overused.length === 0,
        detail: overused.length === 0 ? "every bank line consumed at most once" : `${overused.length} lines over-consumed`
    });
    // ---- cash position ----------------------------------------------------------
    const today = new Date().toISOString().slice(0, 10);
    const matchedByDay = new Map();
    let banked = 0;
    let inTransit = 0;
    for (const m of matches){
        if (!m.settlementId) continue;
        const s = valid.find((x)=>x.id === m.settlementId);
        if (!s || s.amount === null || !s.day) continue;
        if (s.day <= today) banked += s.amount;
        else {
            inTransit += s.amount;
            matchedByDay.set(s.day, (matchedByDay.get(s.day) ?? 0) + s.amount);
        }
    }
    let expectedFromExceptions = 0;
    let atRisk = 0;
    let unpaidInvoices = 0;
    const next7map = new Map();
    const bump = (day, amt, risk)=>{
        const cur = next7map.get(day) ?? {
            expected: 0,
            atRisk: 0
        };
        cur.expected += amt;
        cur.atRisk += risk;
        next7map.set(day, cur);
    };
    for (const [day, amt] of matchedByDay)bump(day, amt, 0);
    for (const e of exceptions){
        if (e.kind === "payment") {
            const p = pays.find((x)=>x.id === e.recordId);
            if (!p) continue;
            const eta = p.day; // predicted net arrival
            const etaDay = new Date(new Date(p.day + "T00:00:00Z").getTime() + 2 * 86400000).toISOString().slice(0, 10);
            if (e.category === "UNSETTLED" || e.category === "AMBIGUOUS") {
                expectedFromExceptions += p.net;
                if (etaDay >= today) bump(etaDay, p.net, e.category === "AMBIGUOUS" ? p.net : 0);
                else atRisk += p.net;
                void eta;
            } else if (e.category === "AMOUNT_MISMATCH") {
                atRisk += Math.abs(p.net);
            }
        }
        if (e.kind === "invoice" && e.category === "UNPAID_INVOICE") {
            const inv = input.invoices.find((x)=>x.id === e.recordId);
            if (inv) {
                unpaidInvoices += inv.amountPaise;
                atRisk += inv.amountPaise;
            }
        }
    }
    const unattributedBank = exceptions.filter((e)=>e.kind === "settlement" && e.category !== "MALFORMED").reduce((acc, e)=>acc + (valid.find((s)=>s.id === e.recordId)?.amount ?? 0), 0);
    const next7 = [];
    for(let i = 0; i < 7; i++){
        const day = new Date(new Date(today + "T00:00:00Z").getTime() + i * 86400000).toISOString().slice(0, 10);
        const cur = next7map.get(day) ?? {
            expected: 0,
            atRisk: 0
        };
        next7.push({
            day,
            label: i === 0 ? "Today" : i === 1 ? "Tmrw" : day.slice(5).split("-").reverse().join("/"),
            expected: cur.expected,
            atRisk: cur.atRisk
        });
    }
    const wallMs = Math.max(1, Math.round(performance.now() - t0));
    const matchedPayments = pays.filter((p)=>p.matched).length;
    const recordsTotal = pays.length + lines.length + input.invoices.length;
    const exceptionsByCategory = {};
    const exceptionsBySeverity = {};
    for (const e of exceptions){
        exceptionsByCategory[e.category] = (exceptionsByCategory[e.category] ?? 0) + 1;
        exceptionsBySeverity[e.severity] = (exceptionsBySeverity[e.severity] ?? 0) + 1;
    }
    const threeWay = pays.filter((p)=>{
        if (!p.matched) return false;
        return matches.some((m)=>m.paymentId === p.id && m.invoiceId);
    }).length;
    const status = invariants.every((i)=>i.ok) ? "completed" : "degraded";
    for (const inv of invariants)mark(inv.ok ? "ok" : "error", `invariant ${inv.name}: ${inv.ok ? "holds" : "VIOLATED"} (${inv.detail})`);
    mark(status === "completed" ? "ok" : "error", `close-out: ${matchedPayments}/${pays.length} payments reconciled · ${exceptions.length} exceptions · run ${status.toUpperCase()} in ${wallMs}ms`);
    return {
        matches,
        exceptions,
        passes,
        trace,
        status,
        invariants,
        stats: {
            payments: pays.length,
            settlements: lines.length,
            validSettlements: valid.length,
            quarantined: broken.length,
            invoices: input.invoices.length,
            matchedPayments,
            partiallyMatched: pays.filter((p)=>p.settlementIds.length > 1).length,
            matchRateBank: pays.length ? matchedPayments / pays.length : 0,
            invoicesMatched: invDones.size,
            matchRateBooks: input.invoices.length ? invDones.size / input.invoices.length : 0,
            threeWay,
            exceptionsOpen: exceptions.length,
            exceptionsByCategory,
            exceptionsBySeverity,
            recordsTotal,
            wallMs,
            throughput: Math.round(recordsTotal / wallMs * 1000)
        },
        cash: {
            today,
            banked,
            inTransit,
            expectedFromExceptions,
            unattributedBank,
            atRisk,
            unpaidInvoices,
            next7
        }
    };
}
const TAG_EXPECTATION = {
    clean: {
        type: "match"
    },
    gross_settle: {
        type: "match"
    },
    mangled_ref: {
        type: "match"
    },
    amount_typo: {
        type: "match"
    },
    delayed_soft: {
        type: "match"
    },
    split: {
        type: "match"
    },
    missing: {
        type: "exception",
        category: "UNSETTLED"
    },
    delayed_hard: {
        type: "exception",
        category: "DATE_DRIFT"
    },
    amount_transposed: {
        type: "exception",
        category: "AMOUNT_MISMATCH"
    },
    collision_a: {
        type: "exception",
        category: "AMBIGUOUS"
    },
    collision_b: {
        type: "exception",
        category: "AMBIGUOUS"
    },
    duplicate: {
        type: "exception",
        category: "DUPLICATE"
    },
    refund: {
        type: "exception",
        category: "ORPHAN_REFUND"
    },
    chargeback: {
        type: "exception",
        category: "ORPHAN_CHARGEBACK"
    },
    orphan: {
        type: "exception",
        category: "ORPHAN_BANK"
    },
    malformed_neg: {
        type: "exception",
        category: "MALFORMED"
    },
    malformed_date: {
        type: "exception",
        category: "MALFORMED"
    },
    malformed_amount: {
        type: "exception",
        category: "MALFORMED"
    },
    unpaid: {
        type: "exception",
        category: "UNPAID_INVOICE"
    }
};
const sideOf = (id)=>id.startsWith("pay_") ? "payment" : id.startsWith("stl_") ? "settlement" : "invoice";
// If an engine flag exists but in a sibling category, the record was still
// surfaced to a human — credit it as handled (not silently missed).
const ALSO_COUNTS = {
    missing: [
        "AMBIGUOUS"
    ],
    delayed_hard: [
        "AMBIGUOUS"
    ],
    amount_transposed: [
        "AMBIGUOUS"
    ]
};
/** Tags whose presence on the same payment overrides a "match" expectation. */ const EXCEPTION_TAGS = new Set([
    "missing",
    "delayed_hard",
    "amount_transposed",
    "collision_a",
    "collision_b"
]);
function scoreRun(result, truth) {
    const enginePairs = new Set();
    const enginePayments = new Map();
    for (const m of result.matches){
        if (!m.paymentId || !m.settlementId) continue;
        enginePairs.add(`${m.paymentId}::${m.settlementId}`);
        enginePayments.set(m.paymentId, [
            ...enginePayments.get(m.paymentId) ?? [],
            m.settlementId
        ]);
    }
    const truthPairs = new Set();
    for (const [payId, stls] of Object.entries(truth.bankLinks)){
        for (const s of stls ?? [])truthPairs.add(`${payId}::${s}`);
    }
    let correctPairs = 0;
    for (const p of enginePairs)if (truthPairs.has(p)) correctPairs++;
    let paymentExact = 0;
    let paymentTotal = 0;
    for (const [payId, stls] of Object.entries(truth.bankLinks)){
        paymentTotal++;
        const want = [
            ...stls ?? []
        ].sort().join("|");
        const got = [
            ...enginePayments.get(payId) ?? []
        ].sort().join("|");
        if (want === got) paymentExact++;
    }
    const ruleMap = new Map();
    const truthBookPairs = new Set();
    for (const [invId, payId] of Object.entries(truth.bookLinks))if (payId) truthBookPairs.add(`${invId}::${payId}`);
    for (const m of result.matches){
        const key = m.rule;
        const cur = ruleMap.get(key) ?? {
            rule: key,
            matched: 0,
            correct: 0,
            incorrect: 0
        };
        cur.matched++;
        if (m.settlementId && m.paymentId) {
            if (truthPairs.has(`${m.paymentId}::${m.settlementId}`)) cur.correct++;
            else cur.incorrect++;
        } else if (m.invoiceId && m.paymentId) {
            if (truthBookPairs.has(`${m.invoiceId}::${m.paymentId}`)) cur.correct++;
            else cur.incorrect++;
        }
        ruleMap.set(key, cur);
    }
    // books
    const engineBook = new Set();
    for (const m of result.matches)if (m.invoiceId && m.paymentId) engineBook.add(`${m.invoiceId}::${m.paymentId}`);
    const truthBook = new Set();
    for (const [invId, payId] of Object.entries(truth.bookLinks))if (payId) truthBook.add(`${invId}::${payId}`);
    let bookCorrect = 0;
    for (const p of engineBook)if (truthBook.has(p)) bookCorrect++;
    const pr = (correct, produced, truthN)=>{
        const precision = produced ? correct / produced : 0;
        const recall = truthN ? correct / truthN : 0;
        const f1 = precision + recall ? 2 * precision * recall / (precision + recall) : 0;
        return {
            precision,
            recall,
            f1
        };
    };
    const bankPR = pr(correctPairs, enginePairs.size, truthPairs.size);
    const bookPR = pr(bookCorrect, engineBook.size, truthBook.size);
    // chaos-deck coverage: was every injected failure handled the right way?
    const rows = [];
    let injectedTotal = 0;
    let detectedTotal = 0;
    const byTag = new Map();
    for (const [recId, tags] of Object.entries(truth.tags)){
        for (const t of tags)byTag.set(t, [
            ...byTag.get(t) ?? [],
            recId
        ]);
    }
    const paymentMatched = new Set(result.matches.filter((m)=>m.paymentId && m.settlementId).map((m)=>m.paymentId));
    const settlementMatched = new Set(result.matches.filter((m)=>m.settlementId).map((m)=>m.settlementId));
    const invoiceMatched = new Set(result.matches.filter((m)=>m.invoiceId).map((m)=>m.invoiceId));
    for (const [tag, ids] of byTag){
        const expect = TAG_EXPECTATION[tag];
        if (!expect) continue;
        // fair-evaluation filter: a payment-level "match" expectation only counts
        // when no exception-tag rides on the same record (a deliberately missing
        // payment is a fact of the workload, not an engine failure).
        const counted = ids.filter((id)=>{
            if (expect.type === "match" && sideOf(id) === "payment") {
                const others = truth.tags[id] ?? [];
                return !others.some((o)=>EXCEPTION_TAGS.has(o));
            }
            return true;
        });
        const missed = [];
        for (const id of counted){
            const side = sideOf(id);
            let ok = false;
            if (expect.category === "DUPLICATE" && side === "payment") {
                // a duplicate payment matching its primary line is correct handling;
                // the duplicate *line* is what must surface as an exception
                ok = paymentMatched.has(id);
            } else if (expect.type === "match") {
                ok = side === "payment" ? paymentMatched.has(id) : side === "settlement" ? settlementMatched.has(id) : invoiceMatched.has(id);
            } else {
                ok = result.exceptions.some((e)=>e.recordId === id && e.category === expect.category);
                if (!ok) {
                    const also = ALSO_COUNTS[tag] ?? [];
                    ok = result.exceptions.some((e)=>e.recordId === id && also.includes(e.category));
                }
            }
            if (!ok) missed.push(id);
        }
        injectedTotal += counted.length;
        detectedTotal += counted.length - missed.length;
        rows.push({
            tag,
            injected: counted.length,
            detected: counted.length - missed.length,
            missed
        });
    }
    rows.sort((a, b)=>a.tag.localeCompare(b.tag));
    return {
        bank: {
            truthPairs: truthPairs.size,
            enginePairs: enginePairs.size,
            correctPairs,
            ...bankPR,
            paymentExact,
            paymentTotal,
            paymentAccuracy: paymentTotal ? paymentExact / paymentTotal : 0
        },
        books: {
            truthPairs: truthBook.size,
            enginePairs: engineBook.size,
            correctPairs: bookCorrect,
            ...bookPR
        },
        rules: [
            ...ruleMap.values()
        ].sort((a, b)=>b.matched - a.matched),
        coverage: {
            injected: injectedTotal,
            detected: detectedTotal,
            rows
        }
    };
}
function heuristicNote(e) {
    switch(e.category){
        case "AMBIGUOUS":
            return "Two payments have identical entitlement to one bank line. No differentiating evidence in-date — do NOT guess. Pull the acquirer reference from the gateway dashboard or wait for the next remittance file, then use manual override.";
        case "AMOUNT_MISMATCH":
            return "Reference matches but money does not. File a short-pay dispute with the acquiring bank quoting the bankRef and expected net; keep the payment flagged until the correction credit arrives.";
        case "UNSETTLED":
            return "No bank line within policy window. Most common cause at this age is a bank holiday queue; if aging crosses T+5, raise a settlement ticket with the UTR and freeze payouts to this MID as a precaution.";
        case "DATE_DRIFT":
            return "Amount matches a far-out bank line. Confirm value date with the bank; if confirmed, post the match manually and tighten the monitoring window for this acquiring route.";
        case "DUPLICATE":
            return "Same reference settled twice. Verify the second credit with the bank statement and initiate a return before it ages past the reversal window.";
        case "ORPHAN_BANK":
            return "Unattributed money is a liability, not a windfall. Park in suspense account 1999 and reconcile against the next gateway cycle; auto-write-off only after 90 days per policy.";
        case "ORPHAN_REFUND":
            return "Refund leg without its original payment inside this batch window. Cross-check the prior batch; link and close.";
        case "ORPHAN_CHARGEBACK":
            return "Chargeback without source payment in-batch. Locate the original payment from the card network reference and open the dispute evidence pack immediately.";
        case "MALFORMED":
            return "Line failed ingestion. Replay from the raw bank file; if the source file itself is corrupt, raise an ops ticket with the bank's settlement desk — do not hand-edit amounts.";
        case "UNPAID_INVOICE":
            return "Invoice past due with no payment. Nudge the customer with a payment link; if paid by another rail, import that statement and re-run.";
        default:
            return "Escalate to the finance-ops queue with this record id attached.";
    }
}
function newRunId() {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["uid"])("run", new __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Rng"](`${Date.now()}-${Math.random()}`));
}
}),
"[project]/src/lib/synth.ts [app-route] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "FEE_BPS",
    ()=>FEE_BPS,
    "METHODS",
    ()=>METHODS,
    "expectedFee",
    ()=>expectedFee,
    "generateBatch",
    ()=>generateBatch
]);
// ---------------------------------------------------------------------------
// REKON synthetic batch generator.
// Produces a realistic 3-source finance workload (gateway payments, a messy
// bank settlement feed, internal invoices) PLUS hidden ground truth used only
// to score the engine afterwards. A deterministic "corruption deck" guarantees
// every batch contains the same failure modes — so accuracy is measured
// against known chaos, never cherry-picked.
// ---------------------------------------------------------------------------
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/util.ts [app-route] (ecmascript)");
;
const FEE_BPS = {
    upi: 30,
    card: 200,
    netbanking: 190,
    wallet: 150
};
const METHODS = Object.keys(FEE_BPS);
function expectedFee(method, amountPaise) {
    const bps = FEE_BPS[method] ?? 0;
    return Math.round(amountPaise * bps / 10000);
}
const CUSTOMERS = [
    "Aarav Textiles",
    "Meera Foods",
    "Zenith Motors",
    "Kaveri Crafts",
    "Nimbus Retail",
    "Orbit Electronics",
    "Saffron Logistics",
    "PixelWorks Studio",
    "Anaya Jewels",
    "Drift Coffee Co",
    "Bolt Fitness",
    "Crescent Pharma",
    "Indus Paper Mill",
    "Lotus Decor",
    "Vista Travels"
];
function mangleUtr(utr, rng, heavy = false) {
    const variants = heavy ? [
        ()=>`IB-${rng.digits(8)}`,
        ()=>utr.slice(-5),
        ()=>"XXXX"
    ] : [
        ()=>`NEFT-${utr.toLowerCase()}`,
        ()=>`UTR: ${utr.slice(0, 6)} ${utr.slice(6)}`,
        ()=>`00${utr}`,
        ()=>utr.slice(0, -2),
        ()=>{
            // transposed characters
            const i = rng.int(4, utr.length - 2);
            return utr.slice(0, i) + utr[i + 1] + utr[i] + utr.slice(i + 2);
        },
        ()=>utr.replace(/^UTR/, "")
    ];
    return rng.pick(variants)();
}
function dayShort(day) {
    return day.slice(2).replace(/-/g, ""); // YYMMDD
}
function asBankDate(day, rng) {
    // bank files are inconsistent: ~40% DD/MM/YYYY, rest ISO
    if (rng.chance(0.4)) {
        const [y, m, d] = day.split("-");
        return `${d}/${m}/${y}`;
    }
    return day;
}
function generateBatch(seed, size = 60, nonce = "") {
    // same seed => same chaos (reproducible measurement); the nonce keeps ids
    // unique so a demo seed can be generated any number of times.
    const rng = new __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Rng"](seed);
    const idRng = new __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Rng"](`${seed}#${nonce}`);
    const batchId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["uid"])("batch", idRng);
    const endDay = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["today"])();
    const startDay = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(endDay, -13);
    // ---- corruption deck: guaranteed coverage, shuffled by the seed ---------
    const scale = Math.max(1, Math.round(size / 60));
    const deck = [
        ...Array(5 * scale).fill("mangled_ref"),
        ...Array(3 * scale).fill("amount_typo"),
        ...Array(2 * scale).fill("amount_transposed"),
        ...Array(3 * scale).fill("delayed_soft"),
        ...Array(2 * scale).fill("delayed_hard"),
        ...Array(6 * scale).fill("missing"),
        ...Array(2 * scale).fill("duplicate"),
        ...Array(2 * scale).fill("split")
    ];
    const shuffled = rng.shuffle(deck).slice(0, size);
    while(shuffled.length < size)shuffled.push("clean");
    // deterministically promote two clean slots into one collision twin pair
    const cleanSlots = shuffled.map((t, i)=>t === "clean" ? i : -1).filter((i)=>i >= 0);
    const posA = cleanSlots.length > 1 ? cleanSlots[rng.int(0, cleanSlots.length - 2)] : -1;
    const posB = posA >= 0 ? cleanSlots.filter((i)=>i !== posA)[rng.int(0, cleanSlots.length - 2)] : -1;
    const payments = [];
    const settlements = [];
    const invoices = [];
    const truth = {
        bankLinks: {},
        bookLinks: {},
        tags: {}
    };
    const tag = (id, t)=>{
        truth.tags[id] = truth.tags[id] ? [
            ...truth.tags[id],
            t
        ] : [
            t
        ];
    };
    let collisionAnchor = null;
    shuffled.forEach((rawTag, i)=>{
        let t = rawTag;
        if (i === posA) t = "collision_a";
        if (i === posB) t = "collision_b";
        const id = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["uid"])("pay", idRng);
        const day = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(startDay, rng.int(0, 12));
        let amountPaise = rng.pick([
            49900,
            99900,
            149900,
            249900,
            499900,
            999900,
            75000,
            1250000,
            350000,
            649900
        ]);
        amountPaise += rng.int(0, 200) * 37 + rng.int(0, 899); // paise-level entropy
        const method = rng.pick(METHODS);
        const customer = rng.pick(CUSTOMERS);
        const utr = `UTR${dayShort(day)}${rng.digits(6)}`;
        const feePaise = expectedFee(method, amountPaise);
        // fee model: 70% settle net of fees, 30% settle gross (fees billed monthly)
        const grossSettle = rng.chance(0.3);
        const settleAmount = grossSettle ? amountPaise : amountPaise - feePaise;
        payments.push({
            id,
            batchId,
            gatewayRef: `pay_${rng.hex(12)}`,
            utr,
            customer,
            method,
            amountPaise,
            feePaise,
            status: "captured",
            day
        });
        tag(id, t);
        if (grossSettle) tag(id, "gross_settle");
        const bankRef = ()=>`SETL-${rng.digits(8)}`;
        const narration = (extra = "")=>`${rng.pick([
                "NEFT",
                "IMPS",
                "RTGS"
            ])} CR RZPY PG SETTLEMENT ${dayShort((0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(day, 1))} ${extra}`.trim();
        const pushLine = (s, tags = [])=>{
            const sid = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["uid"])("stl", idRng);
            settlements.push({
                id: sid,
                batchId,
                ...s
            });
            tags.forEach((x)=>tag(sid, x));
            return sid;
        };
        let link = null;
        if (t === "missing") {
            link = null;
        } else if (t === "split") {
            const a = Math.round(settleAmount * 0.6);
            const b = settleAmount - a;
            const lag = rng.int(1, 2);
            const sDay = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(day, lag);
            const s1 = pushLine({
                bankRef: bankRef(),
                utrRaw: `${utr}-A`,
                amountPaise: a,
                dateRaw: asBankDate(sDay, rng),
                narration: narration("PART 1")
            });
            const s2 = pushLine({
                bankRef: bankRef(),
                utrRaw: `${utr}-B`,
                amountPaise: b,
                dateRaw: asBankDate((0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(day, lag + rng.int(0, 1)), rng),
                narration: narration("PART 2")
            });
            link = [
                s1,
                s2
            ];
        } else if (t === "duplicate") {
            const sDay = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(day, rng.int(1, 2));
            const s1 = pushLine({
                bankRef: bankRef(),
                utrRaw: utr,
                amountPaise: settleAmount,
                dateRaw: asBankDate(sDay, rng),
                narration: narration()
            });
            pushLine({
                bankRef: bankRef(),
                utrRaw: utr,
                amountPaise: settleAmount,
                dateRaw: asBankDate(sDay, rng),
                narration: narration("RETRANSMIT")
            }, [
                "duplicate"
            ]);
            link = [
                s1
            ];
        } else if (t === "collision_a" || t === "collision_b") {
            if (!collisionAnchor) {
                collisionAnchor = {
                    day,
                    amount: amountPaise,
                    method
                };
                // settlement arrives with a bank-internal reference (no resemblance)
                const s1 = pushLine({
                    bankRef: bankRef(),
                    utrRaw: mangleUtr(utr, rng, true),
                    amountPaise: settleAmount,
                    dateRaw: asBankDate((0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(day, 1), rng),
                    narration: narration()
                });
                link = [
                    s1
                ];
            } else {
                // twin payment: same amount, same day, same method — but its settlement
                // never arrives. Two identical entitlements for one bank line.
                const anchor = collisionAnchor;
                const p = payments[payments.length - 1];
                p.amountPaise = anchor.amount;
                p.method = anchor.method;
                p.day = anchor.day;
                p.feePaise = expectedFee(p.method, p.amountPaise);
                collisionAnchor = null;
                link = null;
            }
        } else {
            const lag = t === "delayed_hard" ? 8 : t === "delayed_soft" ? rng.int(3, 5) : rng.int(1, 2);
            const sDay = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(day, lag);
            let lineAmount = settleAmount;
            let utrRaw = utr;
            if (t === "mangled_ref" || t === "delayed_hard") utrRaw = mangleUtr(utr, rng, t === "delayed_hard");
            if (t === "amount_typo") {
                const delta = rng.int(200, 700); // ±₹2–₹7 — bank posting noise
                lineAmount = rng.chance(0.5) ? settleAmount + delta : settleAmount - delta;
            }
            if (t === "amount_transposed") {
                const rs = String(Math.round(settleAmount / 100));
                const i = rng.int(1, rs.length - 1);
                const t2 = rs.slice(0, i) + rs[i + 1] + rs[i] + rs.slice(i + 2);
                lineAmount = parseInt(t2, 10) * 100;
                if (Math.abs(lineAmount - settleAmount) < 2000) lineAmount += 50000; // keep it a real gap
            }
            const s1 = pushLine({
                bankRef: bankRef(),
                utrRaw,
                amountPaise: lineAmount,
                dateRaw: asBankDate(sDay, rng),
                narration: narration(t === "amount_typo" ? "CORR" : "")
            }, t === "mangled_ref" ? [
                "mangled_ref"
            ] : []);
            link = [
                s1
            ];
        }
        truth.bankLinks[id] = link;
    });
    // ---- bank-side junk: orphans and malformed rows (ingest robustness) -----
    const junkDay = ()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(startDay, rng.int(2, 13));
    const junk = [
        {
            kind: "refund",
            utrRaw: `UTR${dayShort(junkDay())}${rng.digits(6)}`,
            amount: -rng.int(20000, 90000),
            nar: "REFUND INITIATED"
        },
        {
            kind: "chargeback",
            utrRaw: `UTR${dayShort(junkDay())}${rng.digits(6)}`,
            amount: rng.int(15000, 80000),
            nar: "CHARGEBACK RAISED"
        },
        {
            kind: "orphan",
            utrRaw: `UTR${dayShort(junkDay())}${rng.digits(6)}`,
            amount: rng.int(10000, 120000),
            nar: "MISC CREDIT"
        },
        {
            kind: "malformed_neg",
            utrRaw: null,
            amount: -4499900,
            nar: "BATCH ADJ"
        },
        {
            kind: "malformed_date",
            utrRaw: `UTR${dayShort(junkDay())}${rng.digits(6)}`,
            amount: 520000,
            nar: "NEFT CR"
        },
        {
            kind: "malformed_amount",
            utrRaw: `UTR${dayShort(junkDay())}${rng.digits(6)}`,
            amount: null,
            nar: "IMPS CR"
        }
    ];
    for (const j of junk){
        const dateRaw = j.kind === "malformed_date" ? "32/13/2026" : asBankDate(junkDay(), rng);
        let amount;
        if (j.kind === "refund" && j.amount !== null) {
            // refund rows are legitimate money-out lines; posted positive with a hint
            amount = Math.abs(j.amount);
        } else {
            amount = j.amount;
        }
        const sid = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["uid"])("stl", idRng);
        settlements.push({
            id: sid,
            batchId,
            bankRef: `SETL-${rng.digits(8)}`,
            utrRaw: j.utrRaw,
            amountPaise: amount,
            dateRaw,
            narration: j.nar
        });
        tag(sid, j.kind);
    }
    // ---- books: invoices ------------------------------------------------------
    let invSeq = 1040 + rng.int(0, 9);
    const invoiceable = rng.shuffle(payments.filter((p)=>truth.bankLinks[p.id] !== null));
    const invoiceCount = Math.round(invoiceable.length * 0.72);
    invoiceable.slice(0, invoiceCount).forEach((p)=>{
        const id = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["uid"])("inv", idRng);
        invoices.push({
            id,
            batchId,
            invoiceNo: `INV-${invSeq++}`,
            customer: p.customer,
            amountPaise: p.amountPaise,
            dueDay: (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(p.day, 7),
            status: "open"
        });
        truth.bookLinks[id] = p.id;
    });
    // two invoices that were never paid -> UNPAID_INVOICE exceptions
    for(let k = 0; k < 2; k++){
        const id = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["uid"])("inv", idRng);
        invoices.push({
            id,
            batchId,
            invoiceNo: `INV-${invSeq++}`,
            customer: rng.pick(CUSTOMERS),
            amountPaise: rng.int(400, 9000) * 100,
            dueDay: (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$util$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addDays"])(endDay, -2),
            status: "open"
        });
        truth.bookLinks[id] = null;
        tag(id, "unpaid");
    }
    return {
        batch: {
            id: batchId,
            name: `BATCH-${endDay.replace(/-/g, "").slice(2)}·${seed.toUpperCase().slice(0, 6)}`,
            seed,
            counts: {
                payments: payments.length,
                settlements: settlements.length,
                invoices: invoices.length
            },
            truth
        },
        payments,
        settlements,
        invoices
    };
}
;
}),
"[project]/src/lib/util.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// ---------------------------------------------------------------------------
// REKON core utilities: seedable RNG, paise-exact formatting, date math.
// ---------------------------------------------------------------------------
__turbopack_context__.s([
    "Rng",
    ()=>Rng,
    "addDays",
    ()=>addDays,
    "compactInt",
    ()=>compactInt,
    "dayDiff",
    ()=>dayDiff,
    "hashSeed",
    ()=>hashSeed,
    "inr",
    ()=>inr,
    "inrShort",
    ()=>inrShort,
    "levenshtein",
    ()=>levenshtein,
    "mulberry32",
    ()=>mulberry32,
    "normalizeRef",
    ()=>normalizeRef,
    "parseBankDate",
    ()=>parseBankDate,
    "pct",
    ()=>pct,
    "refSimilarity",
    ()=>refSimilarity,
    "toDay",
    ()=>toDay,
    "today",
    ()=>today,
    "trigramJaccard",
    ()=>trigramJaccard,
    "uid",
    ()=>uid
]);
function mulberry32(seed) {
    let a = seed >>> 0;
    return function() {
        a |= 0;
        a = a + 0x6d2b79f5 | 0;
        let t = Math.imul(a ^ a >>> 15, 1 | a);
        t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
        return ((t ^ t >>> 14) >>> 0) / 4294967296;
    };
}
function hashSeed(seed) {
    let h = 2166136261;
    for(let i = 0; i < seed.length; i++){
        h ^= seed.charCodeAt(i);
        h = Math.imul(h, 16777619);
    }
    return h >>> 0;
}
class Rng {
    next;
    constructor(seed){
        this.next = mulberry32(typeof seed === "number" ? seed : hashSeed(seed));
    }
    float() {
        return this.next();
    }
    int(min, max) {
        return Math.floor(this.next() * (max - min + 1)) + min;
    }
    pick(arr) {
        return arr[Math.floor(this.next() * arr.length)];
    }
    chance(p) {
        return this.next() < p;
    }
    hex(len) {
        const c = "0123456789ABCDEF";
        let s = "";
        for(let i = 0; i < len; i++)s += c[Math.floor(this.next() * 16)];
        return s;
    }
    digits(len) {
        let s = "";
        for(let i = 0; i < len; i++)s += Math.floor(this.next() * 10);
        return s;
    }
    shuffle(arr) {
        const a = [
            ...arr
        ];
        for(let i = a.length - 1; i > 0; i--){
            const j = Math.floor(this.next() * (i + 1));
            [a[i], a[j]] = [
                a[j],
                a[i]
            ];
        }
        return a;
    }
}
// ------------------------------ dates -------------------------------------
const DAY_MS = 24 * 60 * 60 * 1000;
function toDay(d) {
    return d.toISOString().slice(0, 10);
}
function addDays(day, n) {
    const d = new Date(day + "T00:00:00Z");
    return toDay(new Date(d.getTime() + n * DAY_MS));
}
function dayDiff(a, b) {
    return Math.round((new Date(a + "T00:00:00Z").getTime() - new Date(b + "T00:00:00Z").getTime()) / DAY_MS);
}
function today() {
    return toDay(new Date());
}
function parseBankDate(raw) {
    const iso = /^\d{4}-\d{2}-\d{2}$/;
    if (iso.test(raw)) {
        const d = new Date(raw + "T00:00:00Z");
        return Number.isNaN(d.getTime()) ? null : raw;
    }
    const m = /^(\d{2})\/(\d{2})\/(\d{4})$/.exec(raw);
    if (m) {
        const [, dd, mm, yyyy] = m;
        const d = new Date(`${yyyy}-${mm}-${dd}T00:00:00Z`);
        if (Number.isNaN(d.getTime())) return null;
        const back = toDay(d);
        // reject impossible dates like 32/13/2026 that Date silently rolls over
        if (back !== `${yyyy}-${mm}-${dd}`) return null;
        return back;
    }
    return null;
}
function inr(paise) {
    if (paise === null || paise === undefined) return "—";
    const neg = paise < 0;
    const abs = Math.abs(paise) / 100;
    const s = abs.toLocaleString("en-IN", {
        maximumFractionDigits: 2,
        minimumFractionDigits: 0
    });
    return `${neg ? "−" : ""}₹${s}`;
}
function inrShort(paise) {
    if (paise === null || paise === undefined) return "—";
    const v = paise / 100;
    const abs = Math.abs(v);
    const neg = v < 0 ? "−" : "";
    if (abs >= 10000000) return `${neg}₹${(abs / 10000000).toFixed(2)}Cr`;
    if (abs >= 100000) return `${neg}₹${(abs / 100000).toFixed(1)}L`;
    if (abs >= 1000) return `${neg}₹${(abs / 1000).toFixed(1)}K`;
    return `${neg}₹${abs.toLocaleString("en-IN", {
        maximumFractionDigits: 0
    })}`;
}
function pct(x, digits = 1) {
    return `${(x * 100).toFixed(digits)}%`;
}
function compactInt(n) {
    return n.toLocaleString("en-IN");
}
// --------------------------- reference similarity --------------------------
const REF_PREFIXES = /^(UTR|NEFT|IMPS|RTGS|RZP|REF|NO)+/i;
function normalizeRef(raw) {
    if (!raw) return "";
    let s = raw.toUpperCase().replace(/[^A-Z0-9]/g, "");
    s = s.replace(REF_PREFIXES, "");
    s = s.replace(/^0+/, "");
    return s;
}
function levenshtein(a, b) {
    if (a === b) return 0;
    const m = a.length;
    const n = b.length;
    if (!m) return n;
    if (!n) return m;
    let prev = new Array(n + 1);
    let cur = new Array(n + 1);
    for(let j = 0; j <= n; j++)prev[j] = j;
    for(let i = 1; i <= m; i++){
        cur[0] = i;
        for(let j = 1; j <= n; j++){
            cur[j] = Math.min(prev[j] + 1, cur[j - 1] + 1, prev[j - 1] + (a[i - 1] === b[j - 1] ? 0 : 1));
        }
        [prev, cur] = [
            cur,
            prev
        ];
    }
    return prev[n];
}
function trigramJaccard(a, b) {
    const grams = (s)=>{
        const set = new Set();
        const padded = `**${s}**`;
        for(let i = 0; i < padded.length - 2; i++)set.add(padded.slice(i, i + 3));
        return set;
    };
    if (!a || !b) return 0;
    const A = grams(a);
    const B = grams(b);
    let inter = 0;
    for (const g of A)if (B.has(g)) inter++;
    return inter / (A.size + B.size - inter);
}
function refSimilarity(a, b) {
    if (!a || !b) return 0;
    if (a === b) return 1;
    const lev = levenshtein(a, b);
    const levSim = 1 - lev / Math.max(a.length, b.length);
    return Math.max(levSim, trigramJaccard(a, b));
}
function uid(prefix, rng) {
    const r = rng ? rng.hex(10) : Math.random().toString(16).slice(2, 12).toUpperCase();
    return `${prefix}_${r}`;
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__1nv0sw8._.js.map